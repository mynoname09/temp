# README

Esse projeto utiliza [Next.js](https://nextjs.org)

## Como rodar o projeto

1. Clone o repositório
2. Rode `yarn install` para instalar as dependências
3. Rode `yarn dev` para iniciar o servidor de desenvolvimento
4. Acesse `http://localhost:3000` no seu navegador

## Offtopic

Instalar component shadcn com o yarn 4:

```bash
yarn dlx shadcn@latest add componente_name
```
